// Backend migrado para FastAPI (Python).
// Inicie o servidor com: py -m uvicorn app:app --reload --port 8000
console.log('Backend migrado para FastAPI (Python). Use: py -m uvicorn app:app --reload --port 8000');
process.exit(0);
